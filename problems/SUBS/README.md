# https://rosalind.info/problems/subs/

## Install dependencies

Before running the solution, install the required `stringr` package:

```bash
Rscript -e 'install.packages("stringr", repos="https://cloud.r-project.org")'
```

Then run:

```bash
Rscript solution.R
```